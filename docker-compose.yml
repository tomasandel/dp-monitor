services:
  monitor:
    build: .
    restart: unless-stopped
    environment:
      BACKEND_URL: ${BACKEND_URL}
      MONITOR_ID: ${MONITOR_ID}
      POLL_INTERVAL_MS: ${POLL_INTERVAL_MS}
      MONITOR_API_KEY: ${MONITOR_API_KEY}
      CT_LOG_LIST_URL: ${CT_LOG_LIST_URL}